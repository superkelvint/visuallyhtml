{
  "name": "visualtohtml",
  "private": true,
  "version": "0.1.0",
  "description": "Paste rich text and export cleaned HTML",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "jsdom": "^25.0.1"
  }
}
